import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flex: 1,
    flexDirection: "column",
  },
  btn: {
    backgroundColor: "red",
  },
});

export { styles };
